import React from "react";
import { Text,} from "react-native";
import { firestore, storage } from "../../firebase";

const user =()=>{

    return(

        <Text>

        </Text>
    );

}
export default user;